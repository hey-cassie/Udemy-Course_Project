import { DropdowntwoDirective } from './dropdowntwo.directive';

describe('DropdowntwoDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdowntwoDirective();
    expect(directive).toBeTruthy();
  });
});
